<div class="card p-3">
  <h2 class="mb-3">Reservation Summary</h2>

  <h4>Flight</h4>
  <p>{{flight.flightNumber}} — {{flight.origin}} → {{flight.destination}}</p>
  <p>{{formatDate flight.schedule}} | {{flight.departure}} → {{flight.arrival}}</p>

  <hr>
  <h4>Passenger</h4>
  <p><strong>{{reservation.reserveUser}}</strong></p>
  <p>{{reservation.reserveEmail}}</p>
  <p>{{reservation.passportNo}}</p>

  <hr>
  <h4>Reservation</h4>
  <p><strong>Seat:</strong> {{reservation.seat}}</p>
  <p><strong>Meal:</strong> {{reservation.meal}}</p>
  <p><strong>Baggage:</strong> {{reservation.baggage}}</p>

  <hr>
  <h4>Price</h4>
  <p>Base Fare: ₱5,000</p>
  {{#if baggageFee}}<p>Baggage Fee: ₱{{baggageFee}}</p>{{/if}}
  {{#if mealFee}}<p>Meal Fee: ₱{{mealFee}}</p>{{/if}}
  <h3>Total: ₱{{total}}</h3>

  <div class="mt-3 text-center">
    <a href="/reservations/my-bookings" class="btn btn-primary">Go to My Bookings</a>
    <a href="/flights/Flights" class="btn btn-secondary">Book another flight</a>
  </div>
</div>
